[project]
name = "ariadne"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pyyaml",
    "textual",
]

[dependency-groups]
dev = ["pytest", "ruff"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 88
